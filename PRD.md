# AI 音乐 Podcast 自动生成工具 - 产品需求文档（PRD）

**文档版本**：v3.1  
**创建日期**：2025-03-06  
**产品阶段**：迭代验证中（进入 v3.1）

---

## 迭代记录

- **v1.0（MVP 验证通过）**：端到端自动生成可试听的音乐 Podcast 音频文件。
- **v1.1（迭代一：主持串词插入时序修正）**：
  - **问题**：当前主持串词插入位置依据第一阶段 plan 的 segment 时间，导致部分情况下歌曲尚未播完就开始串词。
  - **变更目标**：串词插入在**每个 segment 之前**（而非 segment 之后）。整期节目**开头先串词再播音乐**，最后一个串词在**最后一个 segment 之前**。主持语音期间不播放背景歌曲。
  - **串词与 segment 顺序（明确）**：若有 N 个 segment，则时间线顺序为 **串词1 → segment 1 → 串词2 → segment 2 → … → 串词N → segment N**。例如 4 个 segment 时：串词1 - segment 1 - 串词2 - segment 2 - 串词3 - segment 3 - 串词4 - segment 4。
  - **混音策略调整**：调整生成顺序为“先排好（歌曲 + 主持语音）时间线，再执行 crossfade”；crossfade 仅对相邻歌曲-歌曲转场生效，不覆盖主持语音段。
  - **Acceptance Criteria（验收标准）**：
    1. 整期开头为串词1，无背景音乐；之后顺序严格为 串词_i → segment_i，串词与歌曲不重叠。
    2. 最后一个 segment 之前为串词N（即结尾顺序为 …串词N - segment N）。
    3. 歌曲-歌曲转场（segment 与 segment 之间）仍保留 crossfade（6–10 秒），且不影响主持语音清晰度。
    4. 生成结果的总时长与原目标时长误差不显著恶化（允许在 MVP 既有容差内波动）。

- **v1.2（迭代二：阶段二严格按 plan 混音）**：
  - **问题**：MVP 中阶段二的选曲流程采用“BPM 过滤 → BPM 排序 → 贪心选曲（时长约束）”，导致最终导出的音乐 mix 与阶段一生成的 plan 不一致。
  - **变更目标**：阶段二不再做选曲与重新排序；严格按照阶段一输出的 plan 来确定歌曲播放顺序与混音顺序，目标是先输出“单集音乐 mix（plan 驱动）”而非优化时长匹配。
  - **时长策略调整**：阶段二阶段的时长限制可先放开，避免为满足时长约束而破坏 plan 一致性；后续迭代再将时长约束迁移到阶段一/整体规划中。
  - **Acceptance Criteria（验收标准）**：
    1. 输出的歌曲播放顺序与 plan 中的顺序一致（不发生 BPM 重排/二次排序）。
    2. 阶段二不再执行“BPM 过滤 + BPM 排序 + 贪心选曲”，mix 决策以 plan 为准。
    3. 若 plan 指定的曲目无法在本地候选库中映射到具体文件，流程应给出清晰错误/缺失提示，而不是自动替换成另一首歌。
    4. 时长误差不再作为阶段二的强约束；相较 v1.1，不要求维持 ±5% 的目标时长精度。

- **v1.3（迭代三：主持串词严格按 plan 歌曲边界插入）**：
  - **问题**：在 v1.2 后阶段二歌曲序列严格按 plan 输出后，主持串词仍按 plan 中每个 segment 的 `target duration seconds` 预估插入位置，导致串词与实际 plan 歌曲时间线出现偏差。
  - **变更目标**：主持串词插入位置不再依赖 `target duration seconds`；而是**严格按 plan 中“歌曲顺序与 segment 的实际拼接结果”**计算插入点。
  - **插入顺序规则（明确）**：若 episode 包含 N 个 segment，则时间线顺序严格为：  
    串词1 - segment 1（歌曲1-歌曲2-歌曲3...）- 串词2 - segment 2（歌曲...）- … - 串词N - segment N（歌曲...）。
  - **混音策略影响**：crossfade 仍保持在“相邻歌曲之间”执行；串词插入点基于已经确定的歌曲时间线与段落边界重新计算，确保串词与歌曲播放严格对齐。
  - **Acceptance Criteria（验收标准）**：
    1. 对任意 segment_i，串词_i 的开始时间与 plan 中 segment_i 对应的**第一首歌播放开始前的边界**一致（不使用 `target duration seconds` 预估）。
    2. 串词与歌曲默认不重叠；串词期间背景音乐静音（v2.1 将在串词结束前执行短时音乐淡入试验）。
    3. 生成结果满足示例顺序：串词1 - segment1（对应歌曲组）- 串词2 - segment2（对应歌曲组）…，不出现“串词跟 plan 对不上”的时间错位问题。
    4. episode 开头先串词1，再播放 segment1；episode 结尾最后一个串词在最后一个 segment 之前。
    5. 若 plan 中任一 segment 的歌曲无法映射到本地文件，流程应报告缺失/阻断生成，避免继续生成导致串词错位。

- **v1.4（迭代四：TTS 供应商切换到 ElevenLabs）**：
  - **问题**：当前使用 Edge TTS 生成主持语音，音质不满足节目发布要求。
  - **变更目标**：将主持语音生成服务从 Edge TTS 切换为 ElevenLabs，以提升语音自然度与整体听感质量。
  - **接入方式说明（待验证）**：当前尚未确定采用何种调用方式（官方 SDK / HTTP API / 直接 request 封装）；v1.4 目标是先打通 ElevenLabs 可用调用链路并完成替换。
  - **User Story（用户视角）**：作为内容创作者，我希望主持串词由更高质量的 TTS 供应商生成，这样整期节目的人声更自然、更接近可发布质量。
  - **功能归类**：优化（音频质量优化与供应商升级）。
  - **Acceptance Criteria（验收标准）**：
    1. 主持语音生成默认使用 ElevenLabs，不再依赖 Edge TTS 作为主路径。
    2. 串词文本可成功通过 ElevenLabs 生成可插入流程的音频文件（格式与采样率满足现有混音链路）。
    3. 若 ElevenLabs 调用失败，系统返回清晰错误信息（不静默失败）。
    4. 现有“串词插入时序与 plan 对齐”逻辑不被破坏（顺序与边界规则保持 v1.3）。
    5. 在同等输入下，主观听感较 v1.3 有可感知提升（以内部试听验收为准）。

- **v2.0（迭代五：串词与歌曲边界 crossfade 试验）**：
  - **问题**：当前串词与歌曲之间没有过渡（crossfade），导致串词听感偏“苍白”、与背景音乐衔接不自然。
  - **变更目标**：在串词与相邻歌曲之间加入短时 crossfade，提升节目整体连贯性，同时保持语音清晰度。
  - **过渡策略**：对“串词开始点/结束点”附近的音乐做淡出/淡入（优先保证语音可懂，crossfade 为可接受的试验版本，默认短时长）。
  - **功能归类**：优化（听感体验优化）。
  - **Acceptance Criteria（验收标准）**：
    1. 在每个“串词开始点”和“串词结束点”附近，音乐进入/离开不再是硬切（可感知的淡入淡出效果）。
    2. 串词语音清晰可懂，且不被背景音乐明显掩盖（内部试听通过）。
    3. 歌曲-歌曲转场仍保留原有 crossfade 行为（不被替换或回退）。
    4. 输出音频不出现明显爆音/削波（clipping），音量变化可控。
    5. 若后续发现效果不佳，可通过配置回退到旧行为（仅边界淡入淡出关闭或置零时长）。

- **v2.1（迭代六：串词清晰度优先的边界过渡）**：
  - **问题**：v2.0 的串词-音乐边界 crossfade 会在“串词淡出/音乐淡入”的重叠期间降低串词可懂度，导致串词听起来不够清晰。
  - **变更目标**：串词保持清晰；在串词结束前让音乐做淡入以保证连贯，同时避免对串词进行淡出。
  - **过渡策略（明确）**：  
    1) 音乐结束后紧接主持串词：不做 crossfade，音乐正常结束后直接开始串词；  
    2) 若串词后面接着音乐：仅在“串词结束前”的尾部对音乐做短时淡入；串词不淡出（不与音乐做反向淡出重叠）。
  - **功能归类**：优化（语音可懂度与听感平衡）。
  - **User Story（用户视角）**：作为内容创作者，我希望串词保持清晰可懂，同时在串词末尾能自然过渡到下一首歌，这样我更容易直接发布成品。
  - **Acceptance Criteria（验收标准）**：
    1. 串词开始时不发生串词淡出（串词音频从开始点保持清晰，不因叠加而降低可懂度）。
    2. 串词结束前出现的过渡只表现为“音乐淡入”，不对串词做淡出；串词与背景音乐重叠期间语音仍可清晰听懂。
    3. 歌曲-歌曲转场 crossfade 行为保持不变。
    4. 输出音频不出现明显爆音/削波；音量变化可控。
    5. 在相同输入下，相比 v2.0 串词可懂度主观提升（内部试听验收为准）。

- **v2.2（迭代七：ThemePlanner prompt 强约束升级）**：
  - **问题**：ThemePlanner 在早期版本中输出的 EpisodePlan 可能不够“贴近真实节目结构”（段落/时长/串词位置等细节偏差），导致后续混音与拼接效果与预期计划不一致。
  - **变更目标**：通过更大篇幅、更强约束的 prompt，提升 LLM EpisodePlan 的结构正确性与可执行性，让返回内容更接近真实可播出的 episode plan。
  - **关键约束（本次 prompt 强化点）**：
    1. **严格 JSON** 输出（不含解释性文字），字段名采用 snake_case。
    2. 允许 `bpm_range`/`overall_bpm_range` 为 `null`（当不方便给出时）。
    3. 统一串词语言：`host_script`（以及可选的段内串词）必须使用用户指定语言。
    4. 总时长约束：所有 segments 的 `target_duration_seconds` 之和需大致接近用户时长（±10%）。
    5. 串词位置约束：包含必须的开场/段落过渡/结尾串词位置；段内相邻歌曲过渡串词可为 `null`。
  - **功能归类**：优化（EpisodePlan 质量与稳定性提升）。
  - **Acceptance Criteria（验收标准）**：
    1. ThemePlanner 返回内容可被稳定解析为严格 JSON（不包含多余文本/Markdown）。
    2. 输出 JSON 字段结构（字段名/层级）与 PRD/代码期望一致（snake_case、必要字段齐全、可空字段遵循定义）。
    3. `target_duration_seconds` 总和与目标时长在 ±10% 内（或按 prompt 设定的“允许浮动”规则满足）。
    4. `host_script`（以及可选的段内串词）使用正确语言；必须的开场/段落过渡/结尾串词位置存在。
    5. 在同等输入下，相比 v2.1，EpisodePlan 的段落结构与串词位置更贴近真实 episode（以内部试听/对比为准）。

- **v3.1（迭代九：阶段一模式可选 + 统一 state_schema.json 输出）**：
  - **问题**：
    1. 阶段一的 `plan-episode` 指令当前代码写死使用多 agent 模式，用户无法选择单 agent 或多 agent。
    2. 无论单 agent 还是多 agent，当前最终返回的仍是 EpisodePlan 格式 JSON，而不是 `state_schema.json` 的同构结构，导致后续链路字段对齐成本高。
  - **变更目标**：
    1. 将多 agent/单 agent 作为用户可选配置（或参数），允许选择不同生成策略。
    2. 无论选择哪种模式，阶段一最终产物输出文件都必须与 `state_schema.json` **同结构**；单 agent 下不涉及的字段填充为 `null`。
  - **功能归类**：优化（可配置性与数据契约一致性提升）。
  - **User Story（用户视角）**：作为内容创作者，我希望能够选择单 agent 或多 agent 生成 EpisodePlan，并且无论选择哪种模式，最终输出都保持与 `state_schema.json` 一致的结构，便于后续混音与调试。
  - **Acceptance Criteria（验收标准）**：
    1. 用户能通过配置/命令参数选择生成模式：`single_agent` 或 `multi_agent`（默认值可由实现指定，但必须可切换）。
    2. 在两种模式下，最终输出文件都能被 `state_schema.json` 的结构约束通过校验（字段层级一致，缺失字段用 `null` 表达）。
    3. 多 agent 模式下生成结果可追踪到 Planner / Music Curator / Script Writer / Critic 的执行状态（至少在 state 的 control/meta 或日志中可定位）。
    4. 若输出 schema 校验失败，系统必须返回明确错误并阻断后续阶段，避免产生不可用 mix。

### 当前生效规则（v3.1）

- 阶段二歌曲顺序严格按 plan 执行，不做 BPM 二次重排/贪心替换。
- 阶段一（plan 生成）支持用户选择 `single_agent` / `multi_agent` 模式。
- 无论单 agent 还是多 agent，阶段一最终输出文件结构都与 `state_schema.json` 同构；单 agent 不涉及字段以 `null` 表达。
- 串词位置按 plan 的 segment 歌曲边界计算：串词_i 在 segment_i 之前（开场先串词）。
- 过渡策略按 v2.1：歌曲→串词不做 crossfade；串词结束前仅音乐淡入；歌曲-歌曲维持 crossfade。
- ThemePlanner 输出遵循强约束 prompt：严格 JSON、snake_case、语言一致、时长与结构可执行。
- TTS 当前主路径为 ElevenLabs。

### 已完成迭代（v3.0）

- **v3.0（迭代八：阶段一 Episode Plan 多 Agent 化）**：
  - **问题**：阶段一目前仍以“单次模型调用”生成 EpisodePlan，存在质量波动、结构失衡、风格不统一、局部难优化的问题。
  - **变更目标**：将阶段一升级为多 Agent Pipeline（Planner / Music Curator / Script Writer / Critic），通过“共享 State + 结构化反馈 + 有限迭代”提升 plan 质量与稳定性。
  - **功能描述（核心）**：
    1. 采用共享 `state`（JSON）作为唯一事实源，Agent 间不自由对话，只读写受控字段。
    2. 引入 Critic Agent 进行结构化评估（评分 + 问题定位 + 修复指令）。
    3. 引入有限次 Refinement Loop（建议最多 2~3 次），达到阈值提前结束。
    4. 增加异常处理：JSON 不合法、缺字段、偏离指令时的 fallback 与重试。
  - **State Schema（v3.0 草案）**：以 `meta / global_constraints / plan / segments / critic / control` 为核心层级；其中 `control.max_iterations` 默认为 3，可配置。
  - **State Schema 完善（本轮）**：补充 `schema_version`、`meta.language/request_id`、`segments.segment_id/order`、`critic.actions[]`（支持多点修复）以及 `control.next_agent/last_updated_by`，提升可追踪性与可编排性。
  - **User Story（用户视角）**：作为内容创作者，我希望 episode plan 由多 Agent 协同生成并可被评估与回修，这样结果更稳定、结构更合理，也更容易按问题定向优化。
  - **功能归类**：新功能（架构升级）+ 优化（质量稳定性提升）。
  - **Acceptance Criteria（验收标准）**：
    1. 阶段一输出不再是单次黑盒结果，流程可追踪到 Planner / Music Curator / Script Writer / Critic 各步骤。
    2. 所有 Agent 输入/输出均为结构化 JSON，并遵循 state schema 的字段约束（仅允许修改自身负责字段）。
    3. Critic 必须输出结构化评估：`pass`、评分维度、问题列表、修复 action（目标 agent + 指令）。
    4. 系统支持有限迭代（max 2~3 次）与提前收敛；超过阈值时给出最终状态与未解决问题。
    5. 异常路径可处理：JSON 非法、缺字段、输出偏离 schema 时可重试或回退，不直接产出不可用 plan。

## 1. 产品背景

### 1.1 行业背景

音乐类 Podcast（类似 Apple Music Radio 的音乐电台节目）是一种新兴的音频内容形态，具有以下特点：

- **单期时长**：60–120 分钟
- **内容结构**：围绕特定主题（情绪 / 风格 / 场景）编排多首歌曲
- **制作要求**：歌曲之间有自然过渡，主持人进行简短介绍或串场

### 1.2 项目背景

当前制作此类节目需要使用专业音频软件（如 Ableton、Logic 等）手动完成全流程：

选曲 → 排序 → 混音 → 录制主持 → 剪辑 → 母带处理

**整个过程耗时较长**，对独立创作者而言效率低下。

本项目旨在开发一个 **AI 驱动的自动 Podcast 生成工具**，通过自动化与 AI 辅助，大幅提高音乐类 Podcast 的制作效率。

### 1.3 产品定位

- **类型**：个人生产效率工具（Creator Tool）
- **阶段**：技术验证（MVP）
- **范围**：个人使用，非面向大众的商业平台

---

## 2. 目标用户

### 2.1 当前版本

| 维度 | 描述 |
|------|------|
| **用户角色** | 独立内容创作者 |
| **用户规模** | 单人（产品开发者本人） |
| **使用频率** | 每周或定期发布 |
| **发布渠道** | 播客平台、音频平台 |

### 2.2 用户画像

- 具备一定的音乐品味和节目策划能力
- 希望提高内容产出效率，将精力集中在创意而非重复性操作
- 能够接受 MVP 阶段的工具限制，优先验证可行性

---

## 3. 用户痛点

| 痛点 | 描述 | 影响 |
|------|------|------|
| **制作耗时长** | 传统流程需手动完成选曲、排序、混音、剪辑等，单期节目制作可能需数小时甚至更久 | 难以维持定期更新节奏 |
| **技术门槛高** | 需掌握专业 DAW 软件（Ableton、Logic 等）及混音、母带知识 | 非专业用户难以入门 |
| **重复性工作多** | 选曲、排序、过渡混音等环节高度重复，可被自动化替代 | 占用创意时间 |
| **主持串词撰写耗时** | 每期需撰写多段串词，且需与节目结构、情绪匹配 | 增加制作负担 |
| **音频一致性难保证** | 多首歌曲音量、响度不一致，需手动母带处理 | 影响听感与专业度 |

---

## 4. 使用场景

### 4.1 核心场景

**场景一：定期节目制作（先拿到歌单规划，再制作）**

- 创作者计划每周发布一期 60 分钟音乐 Podcast
- 每期围绕不同主题（如「Late Night Chill Electronic」「周末晨间爵士」）
- 使用工具生成本期的「节目结构 + 目标歌单规划」
- 创作者根据目标歌单到各流媒体 / 网站搜索并下载对应或类似曲目到本地目录
- 下载完成后，再启动自动制作流程（扫描 → 选曲排序 → 混音 → 主持 → 母带 → 导出）

**场景二：快速试播（只用到前半程规划能力）**

- 创作者想快速验证某个主题/风格的节目方向
- 输入主题和时长，先只使用「主题生成模块」获得节目结构与目标歌单规划
- 可仅使用这份规划作为手工制作或其他工具制作的参考
- 如有需要，再在歌曲准备好后继续使用本工具完成自动制作

**场景三：批量内容储备**

- 创作者希望提前为未来数周制作节目储备
- 一次性为多个主题生成「节目结构 + 目标歌单规划」
- 按规划分批下载、整理歌曲到对应目录
- 歌曲准备完成后，再批量运行自动制作流程，生成多期节目

### 4.2 使用环境

- 本地电脑运行（非 Web 产品）
- 依赖本地音乐库（指定目录下的音频文件），**用户可根据系统生成的目标歌单手动准备和更新此目录**
- 需联网调用 LLM API、TTS API

---

## 5. 用户故事

### 5.1 核心用户故事

| ID | 作为... | 我希望... | 以便... |
|----|---------|-----------|---------|
| US-01 | 内容创作者 | 输入节目主题和期望时长 | 系统能自动生成完整 Podcast 音频 |
| US-02 | 内容创作者 | 系统严格按 plan 的歌曲顺序生成单集 mix（不二次重排） | 导出结果与策划一致，避免播放顺序偏离 |
| US-03 | 内容创作者 | 歌曲之间有平滑过渡 | 听感连贯，无需手动混音 |
| US-04 | 内容创作者 | 系统自动生成主持串词并转为语音 | 我无需录制和剪辑主持部分 |
| US-05 | 内容创作者 | 输出音量统一的完整音频 | 可直接发布，无需额外母带处理 |
| US-06 | 内容创作者 | 获得节目说明文本（Show Notes） | 便于发布时填写节目描述 |
| US-07 | 内容创作者 | 主持串词插入位置与歌曲播放边界一致 | 我听到的串词不会在歌曲未播完时抢先出现 |
| US-08 | 内容创作者 | 主持串词与 plan 的 segment/歌曲边界严格对齐 | 我能听到的串词始终与 plan 策划一致，不会因 target duration 预估偏差而错位 |
| US-09 | 内容创作者 | 串词与歌曲之间具备平滑过渡（crossfade 试验） | 串词更有存在感，与音乐更融为一体 |
| US-10 | 内容创作者 | EpisodePlan 结构与串词位置更接近真实节目 | 我能更少返工，导出结果与 plan 的结构设定更一致 |
| US-11 | 内容创作者 | Episode Plan 由多 Agent 协同并带评估回修机制生成 | 计划质量更稳定且可解释，问题可被定向修复 |

### 5.2 验收标准（以 US-01 为例）

- 用户输入主题（如「Late Night Chill Electronic」）和时长（如 60 分钟）
- 系统在 30–60 分钟内完成全流程
- 输出一个完整 MP3 文件，且播放顺序与 plan 一致（时长误差在当前版本不作为强约束）
- 音频可直接试听，听感基本可用

---

## 6. MVP 功能

### 6.1 功能架构图

```
用户输入（主题 + 时长）
        ↓
┌─────────────────────────────────────────────────────────┐
│  1. 主题生成模块（AI）                                    │
│     → 输出 `state.json`（与 state_schema.json 同结构的 episode plan state） │
└─────────────────────────────────────────────────────────┘
        ↓
     （人工步骤）
        ↓
用户根据「目标歌单规划」到各平台搜索、下载或整理歌曲
        ↓
将准备好的歌曲放入指定本地目录
        ↓
┌─────────────────────────────────────────────────────────┐
│  2. 本地音乐库扫描模块                                    │
│     → 扫描目录、提取时长/BPM 等元数据                       │
└─────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────┐
│  3. 自动选曲与排序模块                                    │
│     → 阶段二按 plan 进行歌曲映射与顺序执行                   │
│     → 不做 BPM 二次排序/贪心重排                             │
└─────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────┐
│  5. 主持语音模块                                          │
│     → 串词插入在每个 segment **之前**：串词1 - Seg1 - 串词2 - Seg2 - … - 串词N - SegN（开场先串词再音乐） │
└─────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────┐
│  4. 自动混音模块                                          │
│     → 先将“歌曲 + 主持语音”按顺序排好，再进行过渡处理       │
│     → 歌曲-歌曲保持 crossfade；串词边界按 v2.1 规则处理      │
└─────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────┐
│  6. 母带处理模块                                          │
│     → 音量统一、Loudness 标准化                            │
└─────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────┐
│  7. 音频导出模块                                          │
│     → 输出完整 MP3 + 可选 Show Notes                      │
└─────────────────────────────────────────────────────────┘
        ↓
输出：完整 Podcast 音频文件
```

### 6.2 功能模块详情

#### 模块 1：主题生成模块（AI）

| 项目 | 说明 |
|------|------|
| **输入** | 用户主题、期望时长 |
| **输出（v3.1）** | `state.json`（与 `state_schema.json` **同结构**的 episode plan state 文件；单 agent 不涉及字段填充为 `null`）。 |
| **实现（v3.1）** | 阶段一支持用户选择 `single_agent` / `multi_agent` 模式；多 agent 模式包含 Planner / Music Curator / Script Writer / Critic + 有限迭代回修；无论模式如何，最终输出均为 `state.json` 并满足 schema 契约。 |
| **使用方式** | 用户可仅运行本模块，先获得节目策划与目标歌单，再根据该规划手动获取或整理歌曲后，继续后续自动化流程 |
| **优先级** | P0 |

#### 模块 2：本地音乐库扫描模块

| 项目 | 说明 |
|------|------|
| **功能** | 扫描指定目录（例如「本期节目专用目录」），读取 mp3/wav 文件，提取元数据 |
| **元数据** | 时长、BPM（至少） |
| **与歌单规划的关系** | 默认假设用户已根据「目标歌单规划」准备好本期候选歌曲，并放入该目录 |
| **实现** | Python 音频分析库 |
| **优先级** | P0 |

#### 模块 3：自动选曲与排序模块

| 项目 | 说明 |
|------|------|
| **选曲规则（v1.2）** | 阶段二不再进行 BPM 过滤/排序后的“重新选曲”；mix 的歌曲序列严格按阶段一输出的 plan 来确定。实现层面允许对 plan 指定的曲目做“本地候选映射”（例如按文件名/元数据最匹配），但不允许改变 plan 顺序。 |
| **排序策略（v1.2）** | 不再进行 BPM 重排；输出/混音顺序严格使用 plan 中的顺序（不做二次排序）。 |
| **MVP 范围（v1.2）** | 以“按照 plan 输出单集音乐 mix”为主，不以时长约束为强目标；复杂情绪算法仍不包含。 |
| **人工介入点** | 若 plan 指定曲目无法成功映射到本地文件，流程应提示缺失（或回退为明确的待处理状态），由用户补齐音乐文件/元数据后再运行；不再通过 BPM 贪心填充替代。 |
| **优先级** | P0 |

#### 模块 4：自动混音模块

| 项目 | 说明 |
|------|------|
| **功能** | 在完成“歌曲 + 主持语音”时间线排布后：1）对相邻歌曲转场做 crossfade；2）在 v2.1 对“串词与相邻歌曲边界”做过渡优化：串词不淡出，音乐仅在“串词结束前”做短时淡入（试验版） |
| **参数** | 歌曲-歌曲 crossfade 维持 6–10 秒；音乐在“串词结束前”的淡入时长默认 2–4 秒（可配置）。串词开始点不进行音乐-串词 crossfade（保持正常切入） |
| **目标** | 保证过渡连贯，同时优先保证串词可懂度，避免 v2.0 中“串词淡出导致清晰度下降”的问题 |
| **MVP 范围** | 不包含 beatmatching、key 匹配 |
| **优先级** | P0 |

#### 模块 5：主持语音模块

| 项目 | 说明 |
|------|------|
| **流程** | AI 生成串词 → TTS 生成语音 → **按 plan 的 segment 所包含歌曲时间线计算插入点（串词在 segment 之前）** |
| **TTS 供应商（v1.4）** | 默认使用 ElevenLabs（替代 Edge TTS）；具体调用路径可为官方 SDK 或 HTTP API（待最终实现验证）。 |
| **要求（v2.1 方案）** | 主持语音期间背景音乐默认静音；在 v2.1：音乐仅在“串词结束前”做短时淡入以增强连贯性，但不对串词做淡出。整期开头先串词再音乐，顺序为 串词1 - segment 1 - 串词2 - segment 2 - … - 串词N - segment N。 |
| **定位策略（关键）** | 串词_i 的开始时间以 plan 中 segment_i 对应的**第一首歌播放开始边界**为准；不再使用 `target duration seconds` 预估插入位置。第一个串词在 episode 开头，最后一个串词在最后一个 segment 之前。 |
| **优先级** | P0 |

#### 模块 6：母带处理模块

| 项目 | 说明 |
|------|------|
| **功能** | 音量统一、Loudness normalization |
| **目标** | 整体听感一致 |
| **优先级** | P0 |

#### 模块 7：音频导出模块

| 项目 | 说明 |
|------|------|
| **输出** | 完整 MP3 文件，时长符合目标 |
| **可选** | Show Notes 文本 |
| **优先级** | P0 |

### 6.3 Agent 输入/输出契约表（v3.0）

| Agent | 可读字段（Read） | 可写字段（Write） | 禁止写字段（Forbidden Write） |
|------|------|------|------|
| **Planner** | `meta`、`global_constraints`、历史 `critic.issues` | `meta.theme_description`、`global_constraints.*`、`plan.segments_design`、`plan.emotion_curve`、`segments[*].segment_id/order/name/target_duration_seconds/bpm_range/mood/segment_design` | `segments[*].playlist`、`segments[*].script`、`critic.*`、`control.*` |
| **Music Curator** | `meta`、`global_constraints`、`plan`、`segments[*].segment_design/mood/bpm_range`、历史 `critic.issues` | `segments[*].playlist`（曲目与顺序） | `plan` 主结构、`segments[*].script`、`critic.*`、`control.max_iterations` |
| **Script Writer** | `meta.language`、`global_constraints`、`plan`、`segments[*].playlist/mood`、历史 `critic.issues` | `segments[*].script.segment_intro`、`segments[*].script.between_tracks` | `segments[*].playlist`、`plan`、`critic.*`、`control.max_iterations` |
| **Critic** | 全量 `state` | `critic.pass`、`critic.scores`、`critic.issues`、`critic.actions`、`control.next_agent` | `meta`、`global_constraints`、`plan`、`segments` 内容本身 |
| **Orchestrator（流程控制）** | 全量 `state` | `control.iteration/status/next_agent/last_updated_by`、重试与回退标记 | 业务内容字段（`plan`、`segments[*].playlist/script`） |

**契约补充规则**
- 所有 Agent 仅允许修改自己负责字段；非负责字段必须原样透传。
- 写入必须是结构化 JSON，禁止自由文本拼接覆盖整个 state。
- Critic 若 `pass=false`，`critic.actions` 必须至少给出 1 条可执行修复指令。
- 当 `control.iteration >= control.max_iterations` 时，Orchestrator 结束回修循环并输出最终状态。

### 6.4 当前版本成功指标（v3.1）

| 指标 | 目标 |
|------|------|
| **制作时间** | 单期节目制作时间 ≤ 30–60 分钟（含用户输入与试听） |
| **输出质量** | 生成音频可直接试听，听感基本可用 |
| **时长准确度** | 当前阶段不作为强约束（以 plan 一致性与可听性为优先）；后续迭代可回收为规划侧目标。 |
| **流程完整性** | 从输入到输出全流程自动化，无需人工干预核心环节 |

---

## 7. 非功能需求

### 7.1 性能需求

| 需求 | 说明 |
|------|------|
| **处理时间** | 单期 60 分钟节目，全流程处理时间目标 < 30 分钟 |
| **资源占用** | 支持在普通个人电脑（8GB+ 内存）上运行 |
| **音频处理** | 支持常见格式（MP3、WAV），采样率 44.1kHz 或 48kHz |

### 7.2 可用性需求

| 需求 | 说明 |
|------|------|
| **交互方式** | 命令行或简易 GUI，输入简单明确 |
| **错误提示** | 关键步骤失败时给出明确错误信息 |
| **日志** | 记录主要步骤执行状态，便于排查问题 |

### 7.3 可维护性需求

| 需求 | 说明 |
|------|------|
| **模块化** | 各功能模块相对独立，便于单独调试与替换 |
| **配置化** | 关键参数（如 BPM 区间、过渡时长、API Key）可配置 |
| **文档** | 提供基本的安装、配置、使用说明 |

### 7.4 兼容性需求

| 需求 | 说明 |
|------|------|
| **运行环境** | 支持 Windows / macOS（优先支持开发者常用环境） |
| **Python** | 基于 Python 3.10+ |
| **依赖** | 依赖清单清晰，可一键安装 |

### 7.5 安全与隐私

| 需求 | 说明 |
|------|------|
| **API Key** | 本地存储，不提交至代码仓库 |
| **音乐文件** | 仅本地读取，不上传至外部服务 |
| **版权** | MVP 阶段不作为重点，由用户自行负责音乐版权 |

---

## 8. 后续扩展（v3+）

### 8.1 高级音频处理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **Key 匹配** | 按调性匹配相邻歌曲，提升听感 | P1 |
| **Beatmatching** | 高级 DJ 式 beatmatching，节拍对齐 | P1 |
| **自动音乐结构识别** | 识别 drop、breakdown 等结构，优化混音点 | P2 |
| **高级情绪曲线** | 更精细的情绪/能量曲线控制 | P2 |

### 8.2 音乐来源扩展

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **在线音乐流媒体接入** | 接入 Spotify、Apple Music 等（需考虑版权与 API 限制） | P2 |
| **多音乐库支持** | 支持多个本地目录、标签分类 | P1 |

### 8.3 产品形态扩展

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **Web 产品界面** | 提供 Web UI，降低使用门槛 | P2 |
| **用户管理系统** | 若面向多用户，需账号、项目管理 | P3 |
| **模板系统** | 预设节目模板（风格、结构），一键生成 | P1 |

### 8.4 内容增强

| 功能 | 描述 | 优先级 |
|------|------|--------|
| **多语言主持** | 支持中英文等不同语言串词与 TTS | P1 |
| **主持风格选择** | 不同语气、语速的 TTS 风格 | P2 |
| **章节标记** | 自动生成章节信息，便于播客平台展示 | P1 |

### 8.5 路线图（示意）

```
Phase 1 (已完成)  → MVP 核心流程打通
Phase 2 (已完成)  → v2.x 计划一致性、串词时序、TTS 与过渡策略迭代
Phase 3 (v3.0+)   → Key 匹配、Beatmatching、多音乐库、模板系统
Phase 4 (未来)    → Web UI、流媒体接入、多用户、高级情绪算法
```

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| **BPM** | Beats Per Minute，每分钟节拍数 |
| **Crossfade** | 交叉淡入淡出，两首歌曲重叠过渡 |
| **Loudness** | 响度，音频感知音量 |
| **TTS** | Text-to-Speech，文本转语音 |
| **Show Notes** | 节目说明文本，用于播客平台描述 |

### B. 参考与约束

- **版权**：MVP 阶段不重点考虑，用户需确保音乐库中的音乐具有合法使用权
- **技术栈**：以 Python 为主，音频处理可使用 librosa、pydub、essentia 等库
- **AI 服务**：LLM 可选 OpenAI、Claude、本地模型等；TTS 当前主路径为 ElevenLabs（替代 Edge TTS），可保留 OpenAI TTS/Coqui 作为后续备选

---

*文档结束*
